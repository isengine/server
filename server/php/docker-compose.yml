version: "3"

services:

  php:
    build: .
    restart: unless-stopped
    env_file:
      - ../../.env
    volumes:
      - ../../project/:/var/www
      - ../.ssh:/.ssh
      - ./conf.d:/usr/local/etc/php/conf.d
      - ./supervisor.d:/etc/supervisor.d
    working_dir: /var/www/
    ports:
      - 9000:9000
    networks:
      - network

networks:
  network:
    name: prod_network
    external: true
